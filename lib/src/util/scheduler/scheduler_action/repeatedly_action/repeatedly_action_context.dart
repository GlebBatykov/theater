part of theater.util;

/// Repeatedly action context is a class which provides information about an action.
class RepeatedlyActionContext extends SchedulerActionContext {
  RepeatedlyActionContext(int counter) : super(counter);
}
